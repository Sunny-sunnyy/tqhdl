# OpenAI API key — dùng cho nút "Strategic Recommendation (AI)" ở Tab 1/2/3
# Copy file này thành .env và điền key thật.
OPENAI_API_KEY=sk-...

# Optional: override model mặc định (gpt-4o-mini)
# OPENAI_MODEL=gpt-4o
